{
    "name": "bods-sirivm-producer",
    "version": "1.0.0",
    "description": "Kafka producer for the SIRI-VM feed, provided by the Bus Open Data Service from the UK Government's Department for Transport",
    "type": "module",
    "scripts": {
        "build": "npm run compile && npm run minify",
        "compile": "tsc",
        "dev": "tsc --pretty && node dist/index.js",
        "minify": "uglifyjs --compress --mangle --output dist/index.js -- dist/index.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [],
    "author": "",
    "license": "UNLICENSED",
    "dependencies": {
        "axios": "^1.7.2",
        "fast-xml-parser": "^4.4.0",
        "kafkajs": "^2.2.4",
        "sax": "^1.4.1",
        "saxpath": "^0.6.5",
        "unzip-stream": "^0.3.4"
    },
    "devDependencies": {
        "@types/node": "^22.0.2",
        "@types/sax": "^1.2.7",
        "@types/unzip-stream": "^0.3.4",
        "typescript": "^5.5.4",
        "uglify-js": "^3.19.1"
    }
}
